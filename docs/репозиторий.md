# Репозиторий

Репозиторий это удалённое хранилище кода с поддержкой системы контроля версий git

Репозиторий имеет следущие параметры:

* id (int) - уникальный идентификатор репозитория
* url - ссылка на репозиторий
* options - неограниченный набор параметров (хранится как JSON)