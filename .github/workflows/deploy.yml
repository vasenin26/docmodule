name: Build and Deploy via SSH

on:
  push:
    tags:
      - 'v*.*.*'  # Запуск только при создании тега vX.X.X

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    environment: Production
    permissions:
      contents: read
      packages: write

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Log in to Container Registry
      uses: docker/login-action@v3
      with:
        registry: ${{ env.REGISTRY }}
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}

    - name: Extract metadata
      id: meta
      uses: docker/metadata-action@v5
      with:
        images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
        tags: |
          type=ref,event=tag
          type=semver,pattern={{version}}
          type=semver,pattern={{major}}.{{minor}}
          type=semver,pattern={{major}}
          type=raw,value=latest,enable={{is_default_branch}}

    - name: Build and push Docker image
      uses: docker/build-push-action@v5
      with:
        context: .
        file: ./docker/php/Dockerfile
        target: prod
        push: true
        tags: ${{ steps.meta.outputs.tags }}
        labels: ${{ steps.meta.outputs.labels }}
        cache-from: type=gha
        cache-to: type=gha,mode=max

    - name: Deploy to server via SSH
      if: startsWith(github.ref, 'refs/tags/v') && github.event_name == 'push'
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ vars.SERVER_HOST }}
        username: ${{ vars.SERVER_USER }}
        key: ${{ secrets.SERVER_SSH_KEY }}
        port: ${{ vars.SERVER_PORT }}
        script: |
          # Создание директории deploy если её нет
          mkdir -p /opt/docmodule
          
          # Копирование файлов конфигурации из репозитория
          curl -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
               -H "Accept: application/vnd.github.v3.raw" \
               -o /opt/docmodule/deploy.sh \
               https://api.github.com/repos/${{ github.repository }}/contents/deploy/deploy.sh?ref=${{ github.ref_name }}
          
          # Делаем скрипт исполняемым
          chmod +x /opt/docmodule/deploy.sh
          
          # Копирование docker-compose файла
          curl -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
               -H "Accept: application/vnd.github.v3.raw" \
               -o /opt/docmodule/docker-compose.prod.yaml \
               https://api.github.com/repos/${{ github.repository }}/contents/docker-compose.prod.yaml?ref=${{ github.ref_name }}
          
          # Переход в директорию и запуск деплоя
          cd /opt/docmodule
          ./deploy.sh deploy ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.ref_name }}
